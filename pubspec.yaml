name: melody
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  animations: ^2.0.2
  calendar_timeline: 1.0.3-null-safety.0
  cupertino_icons: ^1.0.2
  drift: ^1.0.1
  flutter:
    sdk: flutter
  font_awesome_flutter: ^9.2.0
  get: ^4.3.8
  get_storage: ^2.0.3
  path: ^1.8.0
  path_provider: ^2.0.6
  sqlite3_flutter_libs: ^0.5.1

dev_dependencies:
  build_runner: any
  drift_dev: ^1.0.2
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
